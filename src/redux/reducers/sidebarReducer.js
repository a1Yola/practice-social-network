let initialState = {
   friends: [
      { id: 1, name: 'Artem' },
      { id: 2, name: 'Katya' },
      { id: 3, name: 'Sergei' },
   ],
}

const sidebarReducer = (state = initialState, action) => {
   return state
}

export default sidebarReducer
